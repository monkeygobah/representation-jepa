Optional manifests for explicitly curated analysis batches can live here.
The first pipeline version primarily relies on explicit run lists in study
configs, but this directory is reserved for future manifest-based workflows.
